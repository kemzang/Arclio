[install]
linker = "isolated"
# Block packages younger than 7 days (supply-chain protection).
# Mirrors npm `minimum-release-age` (minutes) and pnpm `minimumReleaseAge` (minutes).
minimumReleaseAge = 604800
